{% liquid
  assign animation_effect = settings.animations
  assign animated = false
  if animation_effect != 'none'
    assign animated = true
    if section.settings.animations != 'inherit'
      assign animation_effect = section.settings.animations
    endif
  endif

  assign container = section.settings.container
  if container == 'w-full'
    assign container = 'container-full'
  endif

  assign classes = 'm-section m-product-details-tabs m:block m-gradient m-color-' | append: section.settings.color_scheme
  if section.settings.custom_class != blank
    assign classes = classes | append: ' ' | append: section.settings.custom_class
  endif
%}
{{ 'tabs.css' | asset_url | stylesheet_tag }}
<script src="{{ 'product-details-tabs.js' | asset_url }}" defer="defer"></script>
{%- style -%}
  #m-section-{{ section.id }} {
    --section-padding-top: {{ section.settings.padding_top }}px;
    --section-padding-bottom: {{ section.settings.padding_bottom }}px;
  }
{%- endstyle -%}
<m-product-details-tabs
  id="m-section-{{ section.id }}"
  class="{{ classes }}"
  data-section-type="product-details-tabs"
  data-section-id="{{ section.id }}"
>
  <div class="{{ container }} m-section-py">
    <div class="m-tabs {% if animated %} m-scroll-trigger animate--{{ animation_effect }}{% endif %}">
      <div class="m-tabs__header m:display-flex m:justify-center">
        <div class="m-tabs__header-inner">
          {% for block in section.blocks %}
            <div
              class="m-tab-header"
              data-block-type="{{ block.type }}"
              data-index="{{ forloop.index0 }}"
              {{ block.shopify_attributes }}
            >
              {{ block.settings.header }}
            </div>
          {% endfor %}
        </div>
      </div>
      <div class="m-tabs__content m-product-collapsible">
        {% for block in section.blocks %}
          <div
            class="m-tab-content--{{ block.type }} m-tab-content"
            data-block-type="{{ block.type }}"
          >
            <div class="m-tab-content__inner">
              {% case block.type %}
                {% when 'description' %}
                  <div class="rte">
                    {%- assign description = product.description -%}
                    {{ description }}
                  </div>
                {% when 'reviews' %}
                  <div class="{{ block.settings.container | default: 'container' }}">
                    {% render 'product-reviews-app__snippet', custom_liquid: block.settings.custom_liquid %}
                  </div>
                {% when 'tab' %}
                  <div class="rte">
                    {{ block.settings.content }}
                    {{ block.settings.content_page.content }}
                  </div>
                {% when 'liquid' %}
                  {{ block.settings.custom_liquid }}
                {% else %}

              {% endcase %}
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  </div>
</m-product-details-tabs>
{% schema %}
{
  "name": "产品信息标签",
  "settings": [
    {
      "type": "header",
      "content": "通用"
    },
    {
      "type": "select",
      "id": "container",
      "label": "容器类型",
      "default": "container",
      "options": [
        {
          "value": "container",
          "label": "固定宽度"
        },
        {
          "value": "container-fluid",
          "label": "拉伸宽度"
        },
        {
          "value": "container-narrow",
          "label": "窄宽度"
        }
      ]
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "颜色方案"
    },
    {
      "type": "checkbox",
      "id": "default_open",
      "label": "Default open first tab on mobile 默认在移动端打开第一个标签",
      "default": true
    },
    {
      "type": "header",
      "content": "段落内边距"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "上内边距",
      "default": 50,
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "下内边距",
      "default": 50,
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "自定义属性"
    },
    {
      "type": "text",
      "id": "custom_class",
      "label": "自定义类"
    },
    {
      "type": "header",
      "content": "动画"
    },
    {
      "type": "select",
      "id": "animations",
      "label": "在视图中可见的动画",
      "default": "inherit",
      "options": [
        {
          "value": "inherit",
          "label": "继承全局设置",
        },
        {
          "value": "fade-in",
          "label": "淡入",
        },
        {
          "value": "fade-in-up",
          "label": "淡入向上",
        },
        {
          "value": "fade-in-left",
          "label": "淡入向左",
        },
        {
          "value": "fade-in-right",
          "label": "淡入向右",
        }
      ]
    }
  ],
  "max_blocks": 6,
  "blocks": [
    {
      "type": "description",
      "name": "Product description 产品描述",
      "limit": 1,
      "settings": [
        {
          "type": "paragraph",
          "content": "This tab will show the product's description 这个标签将显示产品描述"
        },
        {
          "type": "text",
          "id": "header",
          "label": "Tab header 标签头部",
          "default": "Product description"
        }
      ]
    },
    {
      "type": "reviews",
      "name": "Product reviews 产品评论",
      "limit": 1,
      "settings": [
        {
          "type": "header",
          "content": "Requirements 要求",
          "info": "This section requires a product reviews app to work. Such as Shopify product reviews, Growave, Judge.me, Loox"
        },
        {
          "type": "text",
          "id": "header",
          "label": "Tab header 标签头部",
          "default": "Product reviews"
        },
        {
          "type": "select",
          "id": "container",
          "label": "容器类型",
          "default": "container",
          "options": [
            {
              "value": "container",
              "label": "固定宽度"
            },
            {
              "value": "container-fluid",
              "label": "拉伸宽度"
            },
            {
              "value": "container-narrow",
              "label": "窄宽度"
            }
          ]
        },
        {
          "type": "header",
          "content": "其他评论应用"
        },
        {
          "type": "liquid",
          "id": "custom_liquid",
          "label": "自定义 liquid",
          "info": "粘贴评论应用的 liquid 代码"
        }
      ]
    },
    {
      "type": "tab",
      "name": "Custom content 自定义内容",
      "settings": [
        {
          "type": "text",
          "id": "header",
          "label": "Tab heading 标签头部",
          "default": "Shipping & Return",
          "placeholder": "Shipping & Return"
        },
        {
          "type": "richtext",
          "id": "content",
          "label": "Tab content 标签内容",
          "default": "<p>Shipping cost is based on weight. Just add products to your cart and use the Shipping Calculator to see the shipping price.</p><p>We want you to be 100% satisfied with your purchase. Items can be returned or exchanged within 30 days of delivery.</p>"
        },
        {
          "type": "page",
          "id": "content_page",
          "label": "Tab content from page 标签内容来自页面",
        }
      ]
    },
    {
      "type": "liquid",
      "name": "Custom liquid 自定义 liquid",
      "settings": [
        {
          "type": "text",
          "id": "header",
          "label": "Tab heading 标签头部",
          "default": "Tab heading"
        },
        {
          "type": "liquid",
          "id": "custom_liquid",
          "label": "Custom liquid 自定义 liquid"
        }
      ]
    }
  ]
}
{% endschema %}
