{{ 'brands-list.css' | asset_url | stylesheet_tag }}
<script src="{{ 'brands-list.js' | asset_url }}" defer="defer"></script>

{% liquid
  assign blocks = section.blocks
  assign items_per_row = section.settings.item_per_row
  assign items_per_row_tablet = items_per_row | divided_by: 2.0 | ceil
  assign enable_slider = false
  if section.blocks.size > items_per_row and section.settings.enable_slider
    assign enable_slider = true
  endif

  assign show_nav = section.settings.show_nav
  assign show_pagination = section.settings.show_pagination

  assign column_gap = section.settings.column_gap
  assign row_gap = section.settings.row_gap
  assign column_gap_mobile = section.settings.column_gap_mobile
  assign row_gap_mobile = section.settings.row_gap_mobile

  assign columns = 6
  if section.settings.container == 'container'
    assign columns = 4
  endif

  if section.blocks.size < columns
    assign columns = section.blocks.size
  endif

  assign animation_effect = settings.animations
  assign animated = false
  if animation_effect != 'none'
    assign animated = true
    if section.settings.animations != 'inherit'
      assign animation_effect = section.settings.animations
    endif
  endif

  assign container = section.settings.container
  if container == 'w-full'
    assign container = 'container-full'
  endif

  assign container_width = settings.container_width
  assign container_fluid_width = settings.container_fluid_width

  assign classes = 'm-section m-brands-list'
  if section.settings.custom_class != blank
    assign classes = classes | append: ' ' | append: section.settings.custom_class
  endif
  assign classes = classes | append: ' m-gradient' | append: ' m-color-' | append: section.settings.color_scheme
%}

{% if section.settings.enable_slider %}
  {%- style -%}
    {% if section.settings.container == 'container' %}
      /* arrow offset x2 + container padding x2 - 1 = 80x2 + 15x2 - 1 */
      @media (max-width: {{ container_width | plus: 189 }}px) {
        #m-brand-list--{{ section.id }} .m-slider-controls--absolute .m-slider-controls__button-prev {
          left: 0;
        }
        #m-brand-list-{{ section.id }} .m-slider-controls--absolute .m-slider-controls__button-next {
          right: 0;
        }
      }
    {% elsif section.settings.container == 'container-fluid' %}
      /* arrow offset x2 + container padding x2 - 1 = 80x2 + 15x2 - 1 */
    @media (max-width: {{ container_fluid_width | plus: 189 }}px) {
      #m-brand-list-{{ section.id }} .m-slider-controls--absolute .m-slider-controls__button-prev {
        left: 0;
      }
      #m-brand-list-{{ section.id }} .m-slider-controls--absolute .m-slider-controls__button-next {
        right: 0;
      }
    }
    {% else %}
      #m-brand-list-{{ section.id }} .m-slider-controls--absolute .m-slider-controls__button-prev {
        left: 0;
      }
      #m-brand-list-{{ section.id }} .m-slider-controls--absolute .m-slider-controls__button-next {
        right: 0;
      }
    {% endif %}
  {%- endstyle -%}
{% endif %}

<style>
  #m-brand-list-{{ section.id }} {
    --items: {{ items_per_row }};
    --column-gap: {{ column_gap }}px;
    --column-gap-mobile: {{ column_gap_mobile }}px;
    --row-gap: {{ row_gap }}px;
    --row-gap-mobile: {{ row_gap_mobile }}px;
    --section-padding-top: {{ section.settings.padding_top }}px;
    --section-padding-bottom: {{ section.settings.padding_bottom }}px;
  }
</style>
<section
  id="m-brand-list-{{ section.id }}"
  class="{{ classes }}"
  data-section-type="brand-list"
  data-section-id="{{ section.id }}"
>
  <div class="{{ container }} m-section-my m-section-py">
    {% render 'index-section-header', section_settings: section.settings %}
    <m-brand-list
      data-enable-slider="{{ enable_slider }}"
      {% if enable_slider %}
        data-enable-autoplay="{{ section.settings.autorotate }}"
        data-autoplay-speed="{{ section.settings.autorotate_speed }}"
        data-items="{{ items_per_row }}"
        data-items-tablet="{{ items_per_row_tablet }}"
      {% endif %}
      class="m-swiper-overflow m:block"
    >
      <div class="m-mixed-layout">
        <div class="m-brands-list__wrapper{% if enable_slider %} swiper-container swiper--equal-height{% endif %}">
          <div
            class="m-brands-list__inner m:grid m:grid-2-cols md:m:grid-{{ items_per_row_tablet }}-cols lg:m:grid-{{ items_per_row }}-cols"
            data-wrapper
          >
            {% for block in blocks %}
              {% liquid
                assign max_width = '100%'
                assign image = block.settings.image
                assign image_by_url = block.settings.image_by_url

                if block.settings.max_width != blank
                  assign max_width = block.settings.max_width
                endif

                if block.settings.image_link != blank
                  assign tag = 'a'
                else
                  assign tag = 'div'
                endif
              %}
              <div class="swiper-slide m:column" data-index="{{ forloop.index0 }}" {{ block.shopify_attributes }}>
                <{{ tag }}
                  {% if block.settings.image_link != blank %}
                    href="{{ block.settings.image_link }}"
                  {% endif %}
                  class="m-brands-list__logo{% if animated %} m-scroll-trigger animate--{{ animation_effect }}{% endif %} m:blocks-radius"
                  {% if animated %}
                    data-cascade
                    style="--animation-order: {{ forloop.index }};"
                  {% endif %}
                >
                  {% if image_by_url != blank %}
                    {% comment %} theme-check-disable RemoteAsset {% endcomment %}
                    <img
                      src="{{ image_by_url }}"
                      style="max-width: {{ max_width }}"
                      width="auto"
                      height="auto"
                      loading="lazy"
                      class="m-image"
                      alt=""
                    >
                    {% comment %} theme-check-enable RemoteAsset {% endcomment %}
                  {% elsif image != blank %}
                    {% render 'responsive-image', image: image, wrapper_class: 'm:w-full', max_width: max_width %}
                  {% else %}
                    {{ 'image' | placeholder_svg_tag: 'm-placeholder-svg m:w-full m:h-full' }}
                  {% endif %}
                </{{ tag }}>
              </div>
            {% endfor %}
          </div>
        </div>
        {% if enable_slider %}
          {% render 'slider-controls',
            show_pagination: show_pagination,
            show_nav: show_nav,
            absolute: true,
            container: container
          %}
        {% endif %}
      </div>
    </m-brand-list>
  </div>
</section>

{% schema %}
{
  "name": "Brands list 品牌列表",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "paragraph",
      "content": "[Watch tutorial](https://foxecom.link/zIc7E8)"
    },
    {
      "type": "header",
      "content": "Section header 段落标题"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading 标题",
      "default": "Brand list"
    },
    {
      "type": "select",
      "id": "heading_size",
      "label": "Heading size 标题大小",
      "default": "h3",
      "options": [
        {
          "label": "Medium 中等",
          "value": "h3"
        },
        {
          "label": "Large 大",
          "value": "h2"
        }
      ]
    },
    {
      "type": "text",
      "id": "subheading",
      "label": "Subheading 副标题"
    },
    {
      "type": "select",
      "id": "header_alignment",
      "label": "Text alignment 文本对齐",
      "default": "center",
      "options": [
        {
          "value": "left",
          "label": "Left 左对齐"
        },
        {
          "value": "center",
          "label": "Center 居中对齐"
        }
      ]
    },
    {
      "type": "header",
      "content": "General 通用"
    },
    {
      "type": "select",
      "id": "container",
      "label": "Container type 容器类型",
      "default": "container-fluid",
      "options": [
        {
          "value": "container-fluid",
          "label": "Default 默认"
        },
        {
          "value": "w-full",
          "label": "Full width 全宽"
        },
        {
          "value": "container",
          "label": "Use container box 使用容器框"
        }
      ]
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme 颜色方案"
    },
    {
      "type": "range",
      "id": "item_per_row",
      "label": "Images per row 每行显示图片数量",
      "min": 2,
      "max": 8,
      "step": 1,
      "default": 4
    },
    {
      "type": "header",
      "content": "Item gaps 图片间距"
    },
    {
      "type": "range",
      "id": "column_gap",
      "label": "Column gap 列间距",
      "min": 0,
      "max": 100,
      "step": 1,
      "default": 30,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "row_gap",
      "label": "Row gap 行间距",
      "min": 0,
      "max": 100,
      "step": 1,
      "default": 30,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "column_gap_mobile",
      "label": "Column gap mobile 移动设备列间距",
      "min": 0,
      "max": 50,
      "step": 1,
      "default": 10,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "row_gap_mobile",
      "label": "Row gap mobile 移动设备行间距",
      "min": 0,
      "max": 50,
      "step": 1,
      "default": 10,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "Slider settings 幻灯片设置"
    },
    {
      "type": "checkbox",
      "id": "enable_slider",
      "label": "Enable slider 启用幻灯片",
      "default": false,
      "info": "Images must be greater than images per row"
    },
    {
      "type": "checkbox",
      "id": "show_nav",
      "label": "Show navigation 显示导航",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "show_pagination",
      "label": "Show pagination 显示分页",
      "default": false
    },
    {
      "type": "checkbox",
      "id": "autorotate",
      "label": "Auto-rotate slides 自动旋转幻灯片",
      "default": false
    },
    {
      "type": "range",
      "id": "autorotate_speed",
      "label": "Change slides every 每秒切换幻灯片",
      "max": 10,
      "min": 2,
      "step": 1,
      "unit": "s",
      "default": 3
    },
    {
      "type": "header",
      "content": "Section Padding 段落内边距"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "Padding Top 上内边距",
      "default": 50,
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "Padding Bottom 下内边距",
      "default": 50,
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "Custom Attributes 自定义属性"
    },
    {
      "type": "text",
      "id": "custom_class",
      "label": "Custom classes 自定义类"
    },
    {
      "type": "header",
      "content": "Animations 动画"
    },
    {
      "type": "select",
      "id": "animations",
      "label": "Visible in the view animation 在视图中可见动画",
      "default": "inherit",
      "options": [
        {
          "value": "inherit",
          "label": "Inherit global setting 继承全局设置"
        },
        {
          "value": "fade-in",
          "label": "Fade in 淡入"
        },
        {
          "value": "fade-in-up",
          "label": "Fade in up 淡入上"
        },
        {
          "value": "fade-in-left",
          "label": "Fade in left 淡入左"
        },
        {
          "value": "fade-in-right",
          "label": "Fade in right 淡入右"
        }
      ]
    }
  ],
  "blocks": [
    {
      "type": "image",
      "name": "Image 图片",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image 图片"
        },
        {
          "type": "text",
          "id": "image_by_url",
          "label": "Image from URL 图片从URL",
          "info": "Enter an image URL with extension .svg, .png or .jpg"
        },
        {
          "type": "url",
          "id": "image_link",
          "label": "Image Link 图片链接"
        },
        {
          "type": "text",
          "id": "max_width",
          "label": "Image width (px) 图片宽度(px)",
          "default": "150px",
          "placeholder": "eg: 100px",
          "info": "Leave blank to use original width. 留空使用原始宽度。"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Brands list 品牌列表",
      "category": "Image 图片",
      "blocks": [
        {
          "type": "image"
        },
        {
          "type": "image"
        },
        {
          "type": "image"
        },
        {
          "type": "image"
        }
      ]
    }
  ]
}
{% endschema %}
