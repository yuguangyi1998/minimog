{% liquid
  assign animation_effect = settings.animations
  assign animated = false
  if animation_effect != 'none'
    assign animated = true
  endif

  assign container = section.settings.container
  if container == 'w-full'
    assign container = 'container-full'
  endif
%}

<div class="m-section m-product-reviews">
  <div class="{{ container }}">
    {%- assign default_heading = 'products.product.product_reviews_heading' | t -%}
    <div class="m-section__header m:text-left">
      <h2 class="m-section__heading h3{% if animated %} m-scroll-trigger animate--{{ animation_effect }}{% endif %}">
        {{ section.settings.heading | default: default_heading }}
      </h2>
    </div>
    {% render 'product-reviews-app__snippet', custom_liquid: section.settings.custom_liquid %}
  </div>
</div>

{% schema %}
{
  "name": "Product reviews",
  "enabled_on": {
    "templates": ["product"]
  },
  "settings": [
    {
      "type": "header",
      "content": "Requirements",
      "info": "This section requires a product reviews app to work. Such as Shopify product reviews, Growave, Judge.me, Loox"
    },
    {
      "type": "select",
      "id": "container",
      "label": "容器类型",
      "default": "container",
      "options": [
        {
          "value": "container",
          "label": "固定宽度"
        },
        {
          "value": "container-fluid",
          "label": "拉伸宽度"
        },
        {
          "value": "container-narrow",
          "label": "窄宽度"
        }
      ]
    },
    {
      "type": "text",
      "id": "heading",
      "label": "标题",
      "default": "Reviews",
      "placeholder": "Reviews"
    },
    {
      "type": "header",
      "content": "其他评论应用"
    },
    {
      "type": "liquid",
      "id": "custom_liquid",
      "label": "自定义 liquid",
      "info": "粘贴评论应用的 liquid 代码"
    }
  ],
  "presets": [
    {
      "name": "产品评论"
    }
  ]
}
{% endschema %}
