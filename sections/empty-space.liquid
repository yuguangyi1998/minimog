{% liquid
  assign container = section.settings.container
%}
{%- style -%}
  .m-section-{{ section.id }} {
    --section-padding-top: {{ section.settings.padding_top }}px;
    --section-padding-bottom: {{ section.settings.padding_bottom }}px;
  }
  .m-divider-section {
    width: 100%;
    border-bottom: {{ section.settings.divider_height }}px {{ section.settings.divider_style }} rgb(var(--color-border));
  }
{%- endstyle -%}
<section
  id="m-section-{{ section.id }}"
  class="m-section-{{ section.id }} m-gradient m-color-{{ section.settings.color_scheme }}"
>
  <div class="{{ container }} m-section-py">
    {% if section.settings.show_divider_line %}
      <div class="m-divider-section"></div>
    {% endif %}
  </div>
</section>

{% schema %}
{
  "name": "Empty space 空空间",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "header",
      "content": "通用"
    },
    {
      "type": "select",
      "id": "container",
      "label": "容器类型",
      "default": "container-fluid",
      "options": [
        {
          "value": "container-fluid",
          "label": "默认"
        },
        {
          "value": "w-full",
          "label": "撑满宽度"
        },
        {
          "value": "container",
          "label": "使用当前区域"
        }
      ]
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "颜色方案"
    },
    {
      "type": "checkbox",
      "id": "show_divider_line",
      "label": "Show divider line",
      "default": true
    },
    {
      "type": "select",
      "id": "divider_style",
      "label": "Divider style",
      "options": [
        {
          "value": "实线",
          "label": "Solid"
        },
        {
          "value": "dotted",
          "label": "Dotted"
        },
        {
          "value": "double",
          "label": "Double"
        },
        {
          "value": "dashed",
          "label": "Dashed"
        }
      ]
    },
    {
      "type": "range",
      "label": "Divider height",
      "id": "divider_height",
      "min": 1,
      "max": 10,
      "step": 1,
      "default": 1,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "段落内边距"
    },
    {
      "type": "range",
      "id": "padding_top",
      "label": "上内边距",
      "default": 50,
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "label": "下内边距",
      "default": 50,
      "min": 0,
      "max": 100,
      "step": 2,
      "unit": "px"
    },
    {
      "type": "header",
      "content": "自定义属性"
    },
    {
      "type": "text",
      "id": "custom_class",
      "label": "自定义类"
    }
  ],
  "presets": [
    {
      "name": "Empty space 空空间"
    }
  ]
}
{% endschema %}
